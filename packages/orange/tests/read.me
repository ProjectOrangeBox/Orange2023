Make sure you add your database settings to the env file in this directory

[phpunit]
host = '127.0.0.1'
database = phpunit
username = phpunit
password = phpunit

This should also contain 

ENVIRONMENT=phpunit

